<Page x:Class="Avrora.Pages.PageApplicationSettings"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Avrora.Pages"
      xmlns:appSettings="clr-namespace.Avrora.ViewModel.ViewModelSettingComponents"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageApplicationSettings">

    <Grid Style="{DynamicResource ForGridStylePageApplicationSettings}"
          DataContext="{StaticResource viewModelSettingsApplicationComponent}">
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock Text="Server: "
                       FontSize="20"
                       Style="{DynamicResource ForTextBlockPageApplicationSettings}"
                       Margin="0 10"/>

            <StackPanel Margin="10">
                
                <StackPanel>
                    <TextBlock 
                               Text="Focus server:"
                               Style="{DynamicResource ForTextBlockPageApplicationSettings}"
                               FontSize="18"
                               Margin="0 5"/>

                    <ComboBox x:Name="ServersCombobox"
                              Margin="30 0 0 0"
                              Width="360"
                              FontSize="16"
                              ItemsSource="{Binding Servers}"
                              SelectedItem="{Binding SelectedServer}">

                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="0 10">
                    <TextBlock Text="Add Server:"
                               Style="{DynamicResource ForTextBlockPageApplicationSettings}"
                               FontSize="18"
                               Margin="0 5"/>
                 
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="TextBoxNameServer"
                                Margin="30 0 10 0"
                                Width="290"
                                FontSize="16"></TextBox>
                        <Button Content="Add"
                                Width="60"
                                FontSize="16"
                                Click="Click_AddServer"/>
                    </StackPanel>
                    
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Delete: "
                               Style="{DynamicResource ForTextBlockPageApplicationSettings}"
                               FontSize="18"
                               Margin="0 5"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock  x:Name="TextBlockDeleteServer"
                                   Text="{Binding SelectedServer}"
                                   Width="290"
                                   Margin="30 0 10 0"
                                   Style="{DynamicResource ForTextBlockPageApplicationSettings}"
                                   FontSize="16"/>
                        <Button Content="Delete" Width="60"
                                FontSize="16"
                                Click="Click_DeleteServer"/>
                    </StackPanel>
                    
                </StackPanel>

                <StackPanel>
                    <TextBlock Margin="10"
                               Text=""
                               Style="{DynamicResource ForTextBlockPageApplicationSettings}"
                               FontSize="14"/>
                </StackPanel>
            </StackPanel>
            
        </StackPanel>
            
    </Grid>
</Page>
